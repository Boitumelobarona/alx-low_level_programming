#include<stdio.h>

/**
  *main-Entry point
  *Return: Always 0(Success)
  */
int main(void)
{
	int = 6;

	if (n > 0)
	{
		printf("\"n is positive);
	}else if (n = o)
	{
		printf("\"n is zero);
	}else
	{
		printf("\"n is negative);
	}

	return (0);
}
