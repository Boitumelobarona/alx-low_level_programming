#include<stdio.h>

/**
  *main-Entry point
  *Return: Always 0 (Success)
  */
int main(void)
{
	int n;

	for (i = 0; i < 100; i++)
	{
		putchar(n + 0);
		putchar(',');
		putchar(' ');
	}
	putchar('\n');
	return (0);
}
